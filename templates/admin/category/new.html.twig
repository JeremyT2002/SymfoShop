{% extends 'admin/base.html.twig' %}

{% block page_title %}New Category{% endblock %}

{% block body %}
    <div class="mb-6">
        <a href="{{ path('admin_categories_index') }}" class="text-gray-600 hover:text-gray-900 mb-2 inline-block">
            <i class="fas fa-arrow-left"></i> Back to Categories
        </a>
        <h2 class="text-2xl font-bold text-gray-900">Create New Category</h2>
    </div>
    
    <div class="admin-card admin-form-shell p-6">
        <form method="post" class="space-y-6">
            <div class="rounded-xl border border-gray-200 bg-gradient-to-br from-gray-50 to-white p-5">
                <h3 class="text-base font-semibold text-gray-900">Category setup</h3>
                <p class="mt-1 text-sm text-gray-600">Create a root category or assign it under an existing parent.</p>
            </div>

            <div class="admin-form-grid">
                <section class="admin-form-section">
                    <label for="name" class="admin-form-label">Category name *</label>
                    <input type="text" id="name" name="name" required placeholder="e.g., Electronics, Clothing, Books" class="admin-form-input" value="{{ category.name|default('') }}">
                    <p class="mt-2 text-xs text-gray-500">Use a short and clear label customers immediately understand.</p>
                </section>

                <section class="admin-form-section">
                    <label for="parent" class="admin-form-label">Parent category</label>
                    <select id="parent" name="parent" class="admin-form-input">
                        <option value="">None (Root Category)</option>
                        {% for cat in allCategories %}
                            <option value="{{ cat.id }}">{{ cat.name }}</option>
                        {% endfor %}
                    </select>
                    <p class="mt-2 text-xs text-gray-500">Optional. Choose a parent to create a subcategory.</p>
                </section>
            </div>

            <div class="flex items-center justify-end gap-3 border-t border-gray-200 pt-5">
                <a href="{{ path('admin_categories_index') }}" class="rounded-lg border border-gray-300 px-4 py-2 text-sm font-medium text-gray-700 hover:bg-gray-50">Cancel</a>
                <button type="submit" class="rounded-lg bg-blue-600 px-4 py-2 text-sm font-semibold text-white hover:bg-blue-700">Create Category</button>
            </div>
        </form>
    </div>
{% endblock %}

